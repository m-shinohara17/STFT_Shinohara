clear; close all; clc;
%各種パラメータ定義、calcSTFTから呼び出し、グラフの表示
%引数は「signalLength」,「fs」「windowLength」,「shiftLength」,返り値はS

inputFileName = "voice.wav";
[audioSig,fs] = audioread(inputFileName); 

signalLength = length(audioSig); % 信号長

windowLength = 4096; % 窓長
shiftLength = windowLength / 2; % シフト長

calcSTFT(signal )